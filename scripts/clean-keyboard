#!/usr/bin/env bash
set -euo pipefail

SECONDS_TO_LOCK="${1:-45}"
ALLOW_ESCAPE="${ALLOW_ESCAPE:-true}"

BIN_PATH="./.build/debug/cleaning-keyboard"

if [[ ! -x "$BIN_PATH" ]]; then
  swift build
fi

"$BIN_PATH" --seconds "$SECONDS_TO_LOCK" --allow-escape "$ALLOW_ESCAPE"
